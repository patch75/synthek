# Session 2026-03-21 — Synthek

## Travaux réalisés

### 1. Restauration git
- `git restore` de tous les fichiers modifiés le matin (projets.js, comparerDocuments.js, Projet.jsx)
- Retour au commit `6110ecb` (session précédente)

### 2. Réorganisation page Projet — ordre des sections
**Avant :** Documents → Bâtiments → Programmes de référence
**Après :** Projet → Intervenants → Bâtiments → Programme - Notices → Documents

- Renommage "Programmes de référence" → "Programme - Notices"
- Ajout section "⚙ Projet" (carte horizontale compacte 6 champs)
- Ajout section "👥 Intervenants"

### 3. Typologies logements — refonte
- Remplacement de `TYPOLOGIES_BASE` :
  - Avant : `['BRS', 'LLS', 'LLTS', 'PLS', 'Accession libre', 'Accession aidée']`
  - Après : `['Social LLS', 'Social LLI', 'Accession BRS', 'Accession standard', 'Accession premium / Attique']`
- Suppression de `BAT A` de la table `TypologiePersonnalisee`
- Reset `batimentsComposition = NULL` sur tous les projets

### 4. Section "👥 Intervenants"
- `ALTER TABLE "Projet" ADD COLUMN "intervenants" TEXT`
- Route `PATCH /projets/:id/intervenants` dans projets.js
- Ajout au schéma Prisma + `prisma generate`
- 9 intervenants fixes : MOA, MOE, Architecte, BET Structure, BET Fluides, BET Électricité, BET VRD, BCT (missions L/S/Ph/Hand/Th/Élec), Économiste
- Lecture seule pour tous, édition inline admin
- Sauvegarde JSON en base

### 5. Section "⚙ Projet" — refonte complète
- `ALTER TABLE "Projet" ADD COLUMN "metadonnees" TEXT`
- `ALTER TABLE "Projet" ADD COLUMN "intervenants" TEXT`
- Ajout au schéma Prisma (`metadonnees`, `intervenants`)
- Route `PATCH /projets/:id` simplifiée : accepte `nom`, `client`, `metadonnees` (JSON)
- Formulaire modal admin refait en 4 blocs :
  1. Identification (nom, MOA, adresse, commune+CP, refs cadastrales, zone climatique)
  2. Nature & programme (type opération radio 4 options, nb bâtiments)
  3. Réglementation thermique (select 6 options RT2012/RE2020 seuils/RT existant, dates PC)
  4. Labels/PLUi (certifications checkboxes, taux EnR, autres exigences)

### 6. Affichage carte horizontale compacte
- 6 champs toujours visibles, ordre : Nom | Type opération | Adresse | MOA | Réglementation | Label
- Valeur vide → `—` en gris
- Version courte des libellés (RE2020 S.2022, LC Neuf, Réhab...)
- Hauteur fixe ~70px

## Bugs corrigés
- `mappingEstValide is not defined` : variable ajoutée ce matin puis restaurée → backend tournait sur ancien code
- `Unknown argument 'metadonnees'` Prisma : champ absent du schema.prisma → ajouté + `prisma generate`
- Connexion ECONNRESET : backend arrêté, redémarré

## Problèmes identifiés (non résolus)
- Feedback Judicieux/Faux positif non persisté en base
- Mapping bâtiment UI (granulométrie) non implémenté côté frontend
- Fast path Claude (appel unique) conditionné à `mappingEstValide` — non activable sans UI granulométrie

## Stack au moment de la session
- Backend : http://localhost:3000 ✅
- Frontend : http://localhost:5173 ✅
- Parser Python : http://127.0.0.1:5001 ✅
- PostgreSQL 18.3 port 5432 ✅
